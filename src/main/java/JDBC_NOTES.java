public class JDBC_NOTES {
    /*
    * JDBC is Api, it is a connectivity tool.
    * Connect to a data source(database)
    * Send queries and update statements to the database
    * then the results received from database

    * CONNECT THE DATA
    * DriverManager.getConnection(host url, username, password)

    * SEND THE QUERIES
    * Send the query written in structured query language (SQL) and process the results.
    *
    * Resultset database ten datalarin geldigi result container
    * Resultset resultset = st.executequery(sql1)

    */
}
